{
  "name": "@magicbutton-cloud/providers",
  "version": "0.1.0",
  "private": true,
  "description": "Magic Button Cloud Providers Monorepo",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r clean"
  },
  "engines": {
    "node": ">=18",
    "pnpm": ">=8"
  }
}